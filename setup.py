import setuptools


setuptools.setup(
    name="edi",
    version="0.1.0",
    description="A package to handle rede edi",
    package_dir={'': 'src'},
    packages=setuptools.find_packages('src'),
    install_requires=[],
)
